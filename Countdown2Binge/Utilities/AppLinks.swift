//
//  AppLinks.swift
//  Countdown2Binge
//
//  Outbound links to the app itself. One place so a share sheet, a review
//  prompt and a marketing string can't drift apart.
//

import Foundation

enum AppLinks {
    /// The App Store listing, appended to shared cards so a friend who sees one
    /// can get the app.
    ///
    static let appStore = URL(string: "https://apps.apple.com/app/id6476959214")
}
